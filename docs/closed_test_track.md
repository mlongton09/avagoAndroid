# Closed Test Track — Avago Android v1.0.0

## Testers to invite
- Internal team: marlong@microsoft.com
- Add additional testers via Play Console → Testing → Internal testing → Manage testers

## Test scenarios
1. Fresh install → provision → sign in → verify assets list loads
2. Create asset → log entry → verify sync to iOS
3. Create work order → assign tech → complete → verify log entry created
4. Inventory receive → use → verify transaction ledger
5. Chat: send message on Android → verify appears on iOS in < 5s
6. Offline: create log entry → go online → verify syncs
7. Switch accounts → verify data isolation
8. Dark mode → verify all screens render correctly
9. German language → verify translation coverage
