package com.avago.feature.settings

import androidx.compose.runtime.Composable
import com.avago.core.ui.EmptyState

/**
 * Members list — placeholder screen.
 * Full implementation (role-based member management, invite flow) is deferred.
 */
@Composable
fun MembersListScreen() {
    EmptyState(message = "Members — coming soon")
}
